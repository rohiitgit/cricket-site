import React from 'react';

const Header = () => {
    return (
        <header className="bg-gradient-to-b from-transparent to-white">
            <h1 className="text-4xl font-bold text-center">WHERE CHAMPIONS RISE</h1>
        </header>
    );
};

export default Header;
